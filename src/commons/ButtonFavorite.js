import { AuthContext } from "../context/auth";
import { useContext } from "react";

const ButtonFavorites = ({ movie }) => {
  return (
    <div className="button">
      
    </div>
  );
};

export default ButtonFavorites;
